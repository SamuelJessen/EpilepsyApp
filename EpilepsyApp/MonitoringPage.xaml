<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EpilepsyApp.MonitoringPage"
             xmlns:viewmodel="clr-namespace:EpilepsyApp.ViewModel"
             x:DataType="viewmodel:MonitoringViewModel"
             Title="">
   <ScrollView>
      <VerticalStackLayout Spacing="0" 
                           Padding="20,0" VerticalOptions="Start">
         <Label VerticalOptions="End"
                HorizontalOptions="End">
            <Label.FormattedText>
               <FormattedString>
                  <Span Text="User: " />
                  <Span Text="{Binding Username}" />
               </FormattedString>
            </Label.FormattedText>
         </Label>

         <Image Source="cortrium.png" Grid.Row="0"  HeightRequest="100" HorizontalOptions="Center" Margin="0,0,0,0" />

         <Button Text="Logout"
                 VerticalOptions="End"
                 HorizontalOptions="End"
                 Grid.Row="1"
                 Command="{Binding LogoutCommand}"/>

      </VerticalStackLayout>
   </ScrollView>
</ContentPage>